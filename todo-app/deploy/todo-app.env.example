# Copy to /etc/todo-app.env  (sudo chmod 640, chown root:todo)
PORT=3000
HOST=127.0.0.1                      # only Caddy on this machine can reach the app
DB_PATH=/var/lib/todo-app/todo.db

# Optional password (browser login popup). Leave both empty for no login.
AUTH_USER=
AUTH_PASS=
